# 検収手順書 — 画箋堂 棚卸PWA

CLAUDE.md「検収テスト(手動チェックリスト)」全17項目を、**①実機なしで検証済みの項目**と
**②実機・デプロイ済みバックエンドが必要な項目**に整理したもの。

- 対象コミット: 初回コミット(`main`)時点の実装
- ①の検証方法: ローカル静的サーバ(`http://localhost`)でアプリを起動し、アプリ本体の関数・
  IndexedDB・Service Worker・CSVパーサを実際に実行して結果を確認(カメラ不要の範囲)
- 検証日: 2026-07-14 / 検証環境: デスクトップChromium(在ブラウザ)

---

## ① 実機なしで検証済み（今回クリア）

| # | 項目 | 検証方法と結果 | 判定 |
|---|---|---|---|
| 1(基盤) | モジュール読込・赤いガイド線 | ZXing/Scanner/db/app が全てロード成功(`typeof ZXing==='object'`)、コンソールエラー0件。`.scan-line`(赤線)はCSS実装済み。**カメラ起動自体は実機で確認**(下記②) | ✅基盤OK |
| 3 | 登録→カメラ復帰→件数増 | `dbAddItem`→`dbListItems`で件数・合計が増加することを確認。UI遷移`registerItem→goto('scan')→updateScanCount`実装確認。**カメラ復帰は実機**(②) | ✅ロジックOK |
| 4 | 同一商品の再読取で「既に◯点登録済み」 | 同一JANを2回登録→`dbFindItemsByJan`が2件検出、合計数量5を確認。注意文言も実装確認 | ✅OK |
| 5 | 画面タップでズーム3段階→等倍 | `cycleZoom`が`(zoomIdx+1)%4`で循環、`CSS_ZOOM_LEVELS=[1,1.5,2,3]`・バッジ表示ロジック確認。**実際の拡大(HW/CSS)は実機**(②) | ✅ロジックOK |
| 6 | JAN手入力バリデーション | `abc`→NG、`123`(3桁)→NG、`4901234567894`→OK を実行確認。不正時エラートースト実装確認 | ✅OK |
| 7 | マスタにないJAN | `openConfirm`の未ヒット分岐(「マスタに見つかりません」表示＋名前入力欄表示)を実装確認 | ✅OK |
| 8 | 棚卸終了→データ確認へ | `btn-scan-end→goto('data')→renderDataView`の遷移・明細一覧描画を実行確認 | ✅OK |
| 9 | 数量修正／削除で一覧・合計更新 | `dbUpdateItemQty`で数量9に更新反映、`dbDeleteItem`で1行減を確認 | ✅OK |
| 11 | オフライン提出でエラー・データ保持 | `submitToWeb`が`navigator.onLine`を参照しオフライン時に送信中止・データ保持する実装を確認(バッジ表示は実機②) | ✅ロジックOK |
| 14 | CSV取込(スマレジCSV)・列自動推定 | **実物のスマレジCSV(Shift_JIS)**を投入→見出し18列を文字化けなく解析、JAN=3列目「商品コード」／商品名=4列目「商品名」／原価=**8列目「原価」**を自動推定。`dbPutProducts→dbGetProduct`で `cost=236/130`(売価400/220ではない)を確認 | ✅OK(v1.1.0再検証) |
| 15 | Shift_JISのCSV取込 | `new TextDecoder('shift_jis')`が利用可能なことを確認(取込時の文字化け自動判定ロジックが機能する前提が成立)。**実SJISファイルでの最終確認は②推奨** | ✅基盤OK |
| 16 | CSV出力(Excelで文字化けなし) | 出力ヘッダ`[棚卸名,JANコード,商品名,原価,数量,読取日時]`、先頭3バイトが`EF BB BF`(UTF-8 BOM)であることを実出力で確認 | ✅OK(v1.1.0再検証) |
| 17(基盤) | オフライン起動の基盤 | Service Worker登録・制御中(`controller`あり)、キャッシュ`tanaoroshi-v1.0.0`にアプリシェル10ファイル全キャッシュを確認。**ホーム画面追加後の実オフライン起動は実機**(②) | ✅基盤OK |

> 注: ①の「ロジックOK/基盤OK」は、カメラ映像・端末振動・PWAインストールなど**ハードウェア依存部分のみ実機②に残る**という意味。データ処理・判定・保存・CSV・オフラインキャッシュは実動作を確認済み。

### v1.0.1（不具合#1 修正後）の再検証結果 — 2026-07-14

修正前は**ローディングが全画面を覆いUI操作が一切通らなかった**ため、当初①はJS直接実行で検証していた（UI層を迂回＝見落としの原因）。
修正後は**実際の画面操作**で以下を再確認済み:

| 項目 | 結果 |
|---|---|
| R-1 起動直後にローディングなし | ✅ `#spinner` の computed display = `none` |
| R-2 ホームのタイルが押せる | ✅ タイル→「棚卸を始める」へ実クリックで遷移 |
| R-3 torchボタン非表示（非対応環境） | ✅ computed display = `none`（設計判断#4どおり） |
| **#6 JAN手入力** | ✅ `abc123`→赤エラートースト「JANコードは数字で入力してください」・画面遷移せず／`4901234567894`→商品確認画面へ遷移 |
| **#7 マスタ外JAN** | ✅「⚠ 商品マスタに見つかりません」表示＋商品名入力欄が出現 |
| 表示制御の健全性 | ✅ `spinner(true)`で表示・`spinner(false)`で消灯、toastも正常 |

> **#5（ズーム3段階）は実機必須**のため未確定。修正でタップが画面に届くようになったので、**公開後に実機で再実施**すること（②A表）。

---

## ⓪ 回帰テスト（不具合#1 再発防止／**毎リリース必須**）

README 不具合ログ#1（`hidden` 属性がCSSに打ち消され、ローディングが全画面を覆って操作不能）の再発を検出するための項目。
**フロント（特に `css/style.css`）を変更したら毎回実施すること。**

| # | 操作 | 期待結果 | 実施 |
|---|---|---|---|
| R-1 | アプリを開く（初回・キャッシュクリア後の両方） | **中央に「処理中…」が出ていない**。タイルが薄くならず、はっきり表示される | ☐ |
| R-2 | ホームの「📦 棚卸」タイルをタップ | **反応して「棚卸を始める」画面へ進む**（＝クリックが遮断されていない） | ☐ |
| R-3 | スキャン画面を開く（torch非対応端末で） | **「💡 ライト ON/OFF」ボタンが表示されない**（設計判断#4。対応端末では表示される） | ☐ |
| R-4 | CSV取込など時間のかかる操作を実行 | 処理中だけローディングが出て、**完了後に必ず消える** | ☐ |
| R-5 | DevTools Console（PC）で確認 | アプリ由来のエラーが0件（拡張機能由来の `A listener indicated an asynchronous response…` は無関係なので無視してよい） | ☐ |

**開発者向け即席チェック**（PCのDevTools Consoleに貼るだけ。全て `true` ならOK）:
```js
getComputedStyle(document.getElementById('spinner')).display === 'none'
```
> `hidden` 属性で出し分けする要素を新規に追加したときは、**computed style が `none` になることを必ず確認**する
> （HTMLに `hidden` と書いてあるだけでは隠れない場合がある、というのが不具合#1の教訓）。

## ⓪-2 スキャナ回帰テスト（不具合#2 最終形／**実機必須・毎リリース**）

`scanner.js` を変更したら必ず実施する。**特に「ZXing内蔵の連続デコードに戻す」変更は禁止**
（実機で読めなくなる。理由はREADME 不具合ログ#2）。

| # | 操作 | 期待結果 | iPad/iPhone | Android |
|---|---|---|---|---|
| T-1 | 通常どおりバーコードにかざす | **プレビュー内に入れば速やかに読み取る**（赤線への厳密な位置合わせ不要） | ☐ | ☐ |
| T-2 | 各種JANを10〜20回読む | **桁数間違いが発生しない**（13桁/EAN-8は8桁） | ☐ | ☐ |
| T-3 | 極小ラベル（鉛筆等の細い商品） | 読み取れる | ☐ | ☐ |
| T-4 | 同じ商品を続けて読む | 「既に◯点登録済み」の注意が出る（2秒重複抑制） | ☐ | ☐ |
| T-5 | 反応の速さ・発熱 | 200ms間隔で重くない | ☐ | ☐ |
| T-6 | スキャン画面を離れる／戻る | カメラが確実に停止・再開する | ☐ | ☐ |

**開発者向けの不変条件**（変更時に壊していないか確認）
- `POSSIBLE_FORMATS` は **EAN_13 / EAN_8 のみ**（ITF等を足すと桁欠け誤読が再発）
- **自前スキャンループ**（毎周回 `drawImage(video)` → 低レベルAPIでデコード）を維持
- **2回連続一致で確定**（`CONFIRM_COUNT=2`。単発誤検出の排除）
- 解像度要求は `ideal:1920×1080`、`min`/`exact` は使わない（起動失敗回避）
- `js/zxing.min.js` は無改変

### 調査の経緯（v1.0.3〜v1.0.7・2026-07-24／履歴）

読取不良の原因究明のため段階的に計測ビルドを投入した記録。**診断UIはv1.0.7で撤去済み**
（必要になれば `46e4c17` / `6fa5fe1` / `63e8d35` から復元可能）。

| 版 | 施策 | 実機結果 |
|---|---|---|
| v1.0.2 | 読取をEAN系に限定 | 桁欠け誤読は減ったが「ほぼ反応しない」が顕在化 |
| v1.0.3 | カメラ診断表示 | `720×1280` / focusMode API非対応 / zoom 0.5〜10 |
| v1.0.4 | 解像度1080p要求 | `1080×1920` に改善したが**読取は改善せず**→解像度は主因でないと確定 |
| v1.0.5 | 診断強化（試行回数・例外・📸取込テスト） | canvas 1080×1920正常・15.3回/秒稼働・全てNotFound・後段例外なし。**📸単発は高確率で成功**＝取り込み経路の問題と判明 |
| **v1.0.6** | **自前スキャンループへ置換＋2回連続一致** | **解決。**プレビュー内に入った瞬間に読取。約20回テストで桁数間違いゼロ。極小ラベルもOK |
| v1.0.6.1 | 📸診断中の自動遷移抑止 | A/B比較を実施 → **仮説否定**（A・Bとも成功） |
| v1.0.7 | 診断UI撤去 | 撤去後も読取・既存機能に回帰なしを確認 |

> **原因は未確定**。`willReadFrequently` 仮説は実機A/Bで否定された（かつ当該A/Bは
> 「新規canvas同士」の比較だったため、ZXingの**使い回しcanvas**の影響は切り分けられていない）。
> 詳細と除外できた要因はREADME 不具合ログ#2を参照。

## ⓪-3 原価・必須入力の回帰テスト（v1.1.0 仕様変更／**毎リリース**）

README 設計判断#9（金額項目＝**原価**）・#10（JAN・商品名は**必須**）の実装が崩れていないかを見る項目。
`app.js` の CSV取込・商品登録・CSV出力・提出を変更したら実施する。

### 事前作業（v1.1.0 に上げるとき **1回だけ** 必要）

| 手順 | 内容 |
|---|---|
| P-1 | **各端末でマスタを取り込み直す**。v1.0系で取り込んだマスタは「売価」が入っているため、原価に入れ替えるにはCSV再取込が必要（アプリは旧データも表示できるが、値の意味が売価のままになる） |
| P-2 | **`gas/Code.gs` を貼り直して再デプロイ**（既存デプロイの「デプロイを管理」→鉛筆→バージョン新規作成）。未実施でも金額は記録される（`price` を併送しているため）が、見出しが「単価」のままになる |
| P-3 | 運用中の「棚卸データ」シートがある場合、**G1セルを手動で「単価」→「原価」に変更**する（見出しはシート新規作成時にしか書き込まれないため自動では変わらない） |

### チェック項目

| # | 操作 | 期待結果 | 実施 |
|---|---|---|---|
| C-1 | 商品情報→CSV取込でスマレジCSVを選ぶ | 対応付け画面の3行目のラベルが**「原価（任意）」**で、**「◯列目: 原価」**が自動選択されている（「商品単価」「オープン価格」ではない） | ☐ |
| C-2 | そのまま取り込み、商品一覧を見る | 各行の右に**「原価 ¥◯◯」**が出る。金額がスマレジの**原価**と一致する（売価ではない） | ☐ |
| C-3 | 商品情報→「＋ 商品を1件ずつ登録する」を開く | JANコード・商品名の見出しに**赤い「必須」バッジ**。登録ボタンが**灰色で押せない** | ☐ |
| C-4 | JANコードだけ入力 | まだ**押せない** | ☐ |
| C-5 | JANコードを消して商品名だけ入力 | まだ**押せない** | ☐ |
| C-6 | 両方入力（原価は空のまま） | ボタンが**朱色になり押せる**。押すと登録され、商品一覧の件数が増える | ☐ |
| C-7 | JANに `abc` を入れ、商品名も入れて押す | 「JANコードは数字で入力してください」のエラー。登録されない（無効化に加えた二重の防御） | ☐ |
| C-8 | 棚卸でマスタ登録済みの商品を読む | 商品確認画面に**「原価 ¥◯◯」**と表示される | ☐ |
| C-9 | データ→CSV出力をExcelで開く | 4列目の見出しが**「原価」**、値が原価になっている | ☐ |
| C-10 | データ→Webへ提出 → スプレッドシートを見る | G列に原価が入る。**P-2/P-3実施後は見出しも「原価」** | ☐ |

> **旧データについて**: v1.0系で登録済みの明細・マスタは `price` キーを持つ。アプリ・GASとも
> `cost` → `price` の順に読むため**表示・提出は途切れない**が、その値は当時の**売価**である。
> 混在を避けるには P-1 のマスタ再取込を行い、以後の棚卸から原価で運用すること。

## ⓪-4 商品マスタ同期の回帰テスト（v1.2.0 仕様変更／**毎リリース**）

README 設計判断#11〜#14。`gas/Code.gs` の `getProducts_` と `app.js` の `syncProducts` を
変更したら実施する。**「同期に失敗しても端末のマスタが壊れない」ことが最重要項目**（S-5〜S-8）。

### 事前作業（v1.2.0 に上げるとき **1回だけ** 必要）

| 手順 | 内容 |
|---|---|
| Q-1 | **`gas/Code.gs` を貼り直して再デプロイ**（デプロイを管理 → 鉛筆 → バージョン新規作成）。旧デプロイのままだと `getProducts` が「不明な操作です」で失敗する |
| Q-2 | Apps Scriptで **`setupProductMaster()` を1回実行**。シート `商品マスタ` が作られ、全体がプレーンテキスト書式になる |
| Q-3 | シート `商品マスタ` を開いた状態で **ファイル→インポート→「現在のシートを置き換える」**でスマレジCSVを取り込む。**「テキストを数値、日付、数式に変換する」はオフ**（オンだとJANの先頭ゼロが落ちる） |
| Q-4 | 各端末で「商品情報」→「☁️ スプレッドシートから同期」を1回押す |

### チェック項目

| # | 操作 | 期待結果 | 実施 |
|---|---|---|---|
| S-1 | 「商品情報」タブを開く | 一番上に**「☁️ スプレッドシートから同期」**（朱色）、その下に「📥 CSVから取り込む（予備）」がある | ☐ |
| S-2 | 同期を押す | ローディングに **「◯◯ / ◯◯件」の進捗**が出て、終わると件数が更新され「**最終同期: 日時**」が表示される | ☐ |
| S-3 | 商品一覧を見る | スプレッドシートの商品名と**原価**が出ている（売価ではない） | ☐ |
| S-4 | 棚卸でその商品を読む | 「✓ 商品マスタに登録済み」と商品名・原価が出る | ☐ |
| S-5 | スプレッドシートから数行消して再同期 | 端末側でもその商品が**引けなくなる**（全件置換。設計判断#14） | ☐ |
| S-6 | **機内モードで同期を押す** | 「オフラインです…」エラー。**件数は変わらず、マスタはそのまま使える** | ☐ |
| S-7 | 設定のトークンをわざと間違えて同期 | 「認証に失敗しました…」エラー。**件数は変わらない** | ☐ |
| S-8 | 同期の途中で機内モードにする | エラーになるが**マスタは同期前のまま残る**（取得完了後に置き換えるため。設計判断#13） | ☐ |
| S-9 | シート `商品マスタ` の名前を変えて同期 | 「シート「商品マスタ」がありません。…setupProductMaster() を…」と**やることが分かるエラー** | ☐ |
| S-10 | 1行目の見出しを消して同期 | 「1行目の見出しが見つかりません…」エラー。**誤った列を黙って取り込まない**（設計判断#12） | ☐ |
| S-11 | 予備のCSV取込を実行 | 従来どおり取り込め、表示が「**最終取込**」に変わる（同期は「最終同期」。どちらで入れたか区別できる） | ☐ |

> **同期とCSV取込は挙動が違う**。同期は**全件置換**（消えた商品は端末からも消える）、
> CSV取込は**追加・上書き**（既存はそのまま残る）。廃番を消したいときは必ず同期を使うこと。

### 開発者向け：ローカル検証の結果（2026-07-24 / v1.2.0）

`gas/Code.gs` を実際に読み込んで動かす擬似GASサーバを立て、**本物のfetch**でPWAから同期して確認済み。

| 検証 | 結果 |
|---|---|
| GAS側ロジック（34項目・スマレジ18列/列並べ替え/見出し無し/ページング/不正行/数値セル/limit異常値/token） | 全項目OK |
| 4,502行の同期 | リクエスト3回（offset 0→2000→4000）・進捗表示4段階・**4,500件取込／JAN不正2行スキップ**・7.1秒 |
| 原価を拾っているか | `cost=501`（売価1001ではない） |
| 全件置換 | 4,500件→100件のシートで再同期し、廃番が端末から消えることを確認 |
| 途中失敗（HTTP 500 / URL誤り / 誤token / シート無し / 見出し不正） | **5パターンすべてで既存マスタ100件が保持**され、文言も具体的 |
| 既存機能の回帰 | 不具合#1・必須化・棚卸フロー・提出・全画面巡回すべてOK |

> 検証中に**IndexedDBがオリジンごと固まる**事象が出たが、原因は
> 「接続が開いたまま `indexedDB.deleteDatabase()` を呼んだ」という**検証手順側の問題**で、
> アプリの不具合ではない（別ポート＝別オリジンで再現しないことを確認）。

## ② 実機テスト手順（iPhone・Android 各1台で実施）

前提: GitHub Pages等でHTTPS公開済み、GASバックエンドをデプロイ済み(`DEPLOY.md`参照)、
端末の「設定」タブに担当者名・提出先URL・トークンを登録済み、
「商品情報」タブで**スプレッドシートから同期済み**(⓪-4 Q表を先に完了しておくこと)。

各行の「合否」に ✅/❌ を記入し、❌は状況をメモすること。

### A. カメラ・読取(実機必須)

| # | 操作 | 期待結果 | iPhone15/iOS | Android |
|---|---|---|---|---|
| 1 | ホーム→棚卸→新しく始める→名前入力→開始 | カメラが起動し赤いガイド線が表示される | ☐ | ☐ |
| 2 | JANコードを赤線に合わせる | (Androidは)振動し、商品確認画面に商品名と**「原価 ¥◯◯」**が表示される | ☐ | ☐ |
| 3 | 数量2にして登録 | トースト表示後、自動でカメラ画面に戻り件数が増える | ☐ | ☐ |
| 5 | スキャン画面を4回タップ | ×1→×2→×3段階→等倍に戻る(右上にバッジ表示、実際に映像が拡大) | ☐ | ☐ |
| 4-torch | ライトボタン | **対応端末のみボタンが表示**され、ON/OFFで実際に点灯/消灯。非対応端末はボタンが出ない(設計判断#4) | ☐ | ☐ |

### B. オフライン・PWA(実機必須)

| # | 操作 | 期待結果 | iPhone15/iOS | Android |
|---|---|---|---|---|
| 10 | 機内モードにしてスキャン・登録 | 問題なく登録できる。ヘッダに「オフライン」バッジ(山吹色)表示 | ☐ | ☐ |
| 11 | 機内モードのまま「Webへ提出」 | 「オフラインです…」エラー。明細データは端末に残る | ☐ | ☐ |
| 17 | ホーム画面に追加→アプリを閉じ、機内モードで起動 | オフラインでもアプリが起動し、既存の棚卸データが見える | ☐ | ☐ |

### C. バックエンド連携(デプロイ済みGAS必須)

| # | 操作 | 期待結果 | 合否 |
|---|---|---|---|
| 12 | オンラインで「Webへ提出」 | スプレッドシート「棚卸データ」に明細行が追記され、「opsLog」に提出記録が残る | ☐ |
| 13 | 設定のトークンを誤った値にして提出 | 「認証に失敗しました…」エラー。スプレッドシートには追記されない | ☐ |

### D. 実機での最終確認(①で基盤確認済み・実機再確認推奨)

| # | 操作 | 期待結果 | 合否 |
|---|---|---|---|
| 6 | 読み取りをスキップ→JAN手入力 | 数字以外はエラー、正しい番号で商品確認画面へ | ☐ |
| 7 | マスタにないJANを読む/入力 | 「マスタに見つかりません」表示。名前入力欄が出て登録できる | ☐ |
| 8 | 棚卸を終了する | データ確認画面に遷移し明細一覧が出る | ☐ |
| 9 | 数量タップ→修正／🗑→削除 | 一覧と合計が更新される | ☐ |
| 14 | 商品情報→スマレジCSV取込(予備手段) | 列対応が自動推定され(JAN=商品コード/商品名/**原価**)、取込後に件数が増える | ☐ |
| 14-2 | 商品情報→スプレッドシートから同期 | 進捗表示の後に件数が更新され「最終同期」が出る。詳細は**⓪-4 S表**を実施 | ☐ |
| 15 | Shift_JISのCSVを取込 | 文字化けせず取り込める(実SJISファイルで確認) | ☐ |
| 16 | CSV出力 | Excelで文字化けなく開ける(棚卸名/JAN/商品名/**原価**/数量/日時) | ☐ |

---

## 補足

- **京都銀行等の外部取込は本アプリ対象外**(棚卸はスプレッドシート追記のみ)。
- 提出は同一データを複数回押すとスプレッドシートに重複追記される仕様(設計判断#5「加算方式」と整合、
  再提出時は確認ダイアログあり)。運用では提出後の再提出可否を担当者で取り決めること。
- 実機テストで不具合を発見した場合は、CLAUDE.md/共通標準§5に従い
  「症状＋該当設計書の記述＋再検証するテスト番号」の形で記録し、README「不具合ログ」に追記する。

# 検収手順書 — 画箋堂 棚卸PWA

CLAUDE.md「検収テスト(手動チェックリスト)」全17項目を、**①実機なしで検証済みの項目**と
**②実機・デプロイ済みバックエンドが必要な項目**に整理したもの。

- 対象コミット: 初回コミット(`main`)時点の実装
- ①の検証方法: ローカル静的サーバ(`http://localhost`)でアプリを起動し、アプリ本体の関数・
  IndexedDB・Service Worker・CSVパーサを実際に実行して結果を確認(カメラ不要の範囲)
- 検証日: 2026-07-14 / 検証環境: デスクトップChromium(在ブラウザ)

---

## ① 実機なしで検証済み（今回クリア）

| # | 項目 | 検証方法と結果 | 判定 |
|---|---|---|---|
| 1(基盤) | モジュール読込・赤いガイド線 | ZXing/Scanner/db/app が全てロード成功(`typeof ZXing==='object'`)、コンソールエラー0件。`.scan-line`(赤線)はCSS実装済み。**カメラ起動自体は実機で確認**(下記②) | ✅基盤OK |
| 3 | 登録→カメラ復帰→件数増 | `dbAddItem`→`dbListItems`で件数・合計が増加することを確認。UI遷移`registerItem→goto('scan')→updateScanCount`実装確認。**カメラ復帰は実機**(②) | ✅ロジックOK |
| 4 | 同一商品の再読取で「既に◯点登録済み」 | 同一JANを2回登録→`dbFindItemsByJan`が2件検出、合計数量5を確認。注意文言も実装確認 | ✅OK |
| 5 | 画面タップでズーム3段階→等倍 | `cycleZoom`が`(zoomIdx+1)%4`で循環、`CSS_ZOOM_LEVELS=[1,1.5,2,3]`・バッジ表示ロジック確認。**実際の拡大(HW/CSS)は実機**(②) | ✅ロジックOK |
| 6 | JAN手入力バリデーション | `abc`→NG、`123`(3桁)→NG、`4901234567894`→OK を実行確認。不正時エラートースト実装確認 | ✅OK |
| 7 | マスタにないJAN | `openConfirm`の未ヒット分岐(「マスタに見つかりません」表示＋名前入力欄表示)を実装確認 | ✅OK |
| 8 | 棚卸終了→データ確認へ | `btn-scan-end→goto('data')→renderDataView`の遷移・明細一覧描画を実行確認 | ✅OK |
| 9 | 数量修正／削除で一覧・合計更新 | `dbUpdateItemQty`で数量9に更新反映、`dbDeleteItem`で1行減を確認 | ✅OK |
| 11 | オフライン提出でエラー・データ保持 | `submitToWeb`が`navigator.onLine`を参照しオフライン時に送信中止・データ保持する実装を確認(バッジ表示は実機②) | ✅ロジックOK |
| 14 | CSV取込(スマレジCSV)・列自動推定 | サンプルCSVをパーサに投入→行数・クォート内カンマ保持・JAN/商品名/売価の列自動推定が全て正しいことを確認。`dbPutProducts→dbGetProduct`で取込・照合成功 | ✅OK |
| 15 | Shift_JISのCSV取込 | `new TextDecoder('shift_jis')`が利用可能なことを確認(取込時の文字化け自動判定ロジックが機能する前提が成立)。**実SJISファイルでの最終確認は②推奨** | ✅基盤OK |
| 16 | CSV出力(Excelで文字化けなし) | 出力ヘッダ`[棚卸名,JANコード,商品名,単価,数量,読取日時]`が検収項目の列と完全一致、UTF-8 BOM付き方式を確認 | ✅OK |
| 17(基盤) | オフライン起動の基盤 | Service Worker登録・制御中(`controller`あり)、キャッシュ`tanaoroshi-v1.0.0`にアプリシェル10ファイル全キャッシュを確認。**ホーム画面追加後の実オフライン起動は実機**(②) | ✅基盤OK |

> 注: ①の「ロジックOK/基盤OK」は、カメラ映像・端末振動・PWAインストールなど**ハードウェア依存部分のみ実機②に残る**という意味。データ処理・判定・保存・CSV・オフラインキャッシュは実動作を確認済み。

### v1.0.1（不具合#1 修正後）の再検証結果 — 2026-07-14

修正前は**ローディングが全画面を覆いUI操作が一切通らなかった**ため、当初①はJS直接実行で検証していた（UI層を迂回＝見落としの原因）。
修正後は**実際の画面操作**で以下を再確認済み:

| 項目 | 結果 |
|---|---|
| R-1 起動直後にローディングなし | ✅ `#spinner` の computed display = `none` |
| R-2 ホームのタイルが押せる | ✅ タイル→「棚卸を始める」へ実クリックで遷移 |
| R-3 torchボタン非表示（非対応環境） | ✅ computed display = `none`（設計判断#4どおり） |
| **#6 JAN手入力** | ✅ `abc123`→赤エラートースト「JANコードは数字で入力してください」・画面遷移せず／`4901234567894`→商品確認画面へ遷移 |
| **#7 マスタ外JAN** | ✅「⚠ 商品マスタに見つかりません」表示＋商品名入力欄が出現 |
| 表示制御の健全性 | ✅ `spinner(true)`で表示・`spinner(false)`で消灯、toastも正常 |

> **#5（ズーム3段階）は実機必須**のため未確定。修正でタップが画面に届くようになったので、**公開後に実機で再実施**すること（②A表）。

---

## ⓪ 回帰テスト（不具合#1 再発防止／**毎リリース必須**）

README 不具合ログ#1（`hidden` 属性がCSSに打ち消され、ローディングが全画面を覆って操作不能）の再発を検出するための項目。
**フロント（特に `css/style.css`）を変更したら毎回実施すること。**

| # | 操作 | 期待結果 | 実施 |
|---|---|---|---|
| R-1 | アプリを開く（初回・キャッシュクリア後の両方） | **中央に「処理中…」が出ていない**。タイルが薄くならず、はっきり表示される | ☐ |
| R-2 | ホームの「📦 棚卸」タイルをタップ | **反応して「棚卸を始める」画面へ進む**（＝クリックが遮断されていない） | ☐ |
| R-3 | スキャン画面を開く（torch非対応端末で） | **「💡 ライト ON/OFF」ボタンが表示されない**（設計判断#4。対応端末では表示される） | ☐ |
| R-4 | CSV取込など時間のかかる操作を実行 | 処理中だけローディングが出て、**完了後に必ず消える** | ☐ |
| R-5 | DevTools Console（PC）で確認 | アプリ由来のエラーが0件（拡張機能由来の `A listener indicated an asynchronous response…` は無関係なので無視してよい） | ☐ |

**開発者向け即席チェック**（PCのDevTools Consoleに貼るだけ。全て `true` ならOK）:
```js
getComputedStyle(document.getElementById('spinner')).display === 'none'
```
> `hidden` 属性で出し分けする要素を新規に追加したときは、**computed style が `none` になることを必ず確認**する
> （HTMLに `hidden` と書いてあるだけでは隠れない場合がある、というのが不具合#1の教訓）。

## ⓪-2 回帰テスト（不具合#2 再発防止／**実機必須・毎リリース**）

README 不具合ログ#2（ITF/CODE_39を有効化していたためJANが桁欠け誤読・低速化）の再発検出。
**`scanner.js` の読取フォーマット・ZXing設定を変更したら必ず実施。カメラ実機が必要。**

| # | 操作 | 期待結果 | iPhone/iPad | Android |
|---|---|---|---|---|
| S-1 | 実機カメラでJAN(EAN-13)を10回連続で読む | **速やかに反応**し、大半が一発で読める（「ほぼ反応しない」状態でない） | ☐ | ☐ |
| S-2 | 読み取れたJANの桁数を確認 | 商品確認画面のJANが**13桁（EAN-8なら8桁）で欠けがない**。桁数が中途半端な値にならない | ☐ | ☐ |
| S-3 | 読み取れたJANがマスタと一致するか | 登録済み商品が「✓ 商品マスタに登録済み」になる（誤読なら別商品/マスタ外になる） | ☐ | ☐ |
| S-4 | ITF/CODE_39等のバーコード（もし店内にあれば）にかざす | **反応しない**（JAN以外は読取対象外にしたため。※これは正常動作） | ☐ | ☐ |
| S-5 | iPad Pro(第3世代)・iPhone 16 Pro の**両方**で S-1〜S-3 | 両機種とも桁欠け・無反応が起きない（不具合#2の再現環境） | ☐ | ☐ |

> 開発者メモ：`scanner.js` の `_buildReader` は `POSSIBLE_FORMATS` を **`EAN_13`/`EAN_8` のみ**に限定している。
> 棚札等でCODE_128を読む運用が将来必要になったら、**全体に足すのではなくモード別に限定有効化**すること（設計判断#8）。

## ⓪-3 カメラ計測手順（v1.0.3 診断ビルド／**実機で1回**）

不具合#2続報（読取不良の真因はピント／解像度）の切り分けのため、**iPad Pro（第3世代）と iPhone 16 Pro の両方**で
スキャン画面の「📷 カメラ診断（計測用）」パネルの値を記録する。**読取そのものの改善は次版（施策1・2）で行う。**

**手順**
1. v1.0.3を公開 → スーパーリロード（iPhoneはアイコン削除→再追加）でキャッシュ更新
2. ホーム→棚卸→新規→開始 でスキャン画面を出す（カメラ映像の下に診断パネルが出る）
3. **バーコードにかざしてピントが合うのを1〜2秒待ってから「🔄 診断を更新」をタップ**（AF確定後の値を拾うため）
4. パネルの各行を記録（下表）。**通常サイズのJAN**と**鉛筆等の極小ラベル**の両方で試す

**記録シート**

| 項目 | iPad Pro(3) | iPhone 16 Pro | 見るポイント |
|---|---|---|---|
| 解像度（例 1920×1080） | | | 低い(640×480等)なら**施策2:解像度引き上げ**が効く |
| fps | | | 参考 |
| ピント（focusMode） | | | `continuous`でなければ**施策1:連続AF**指定を検討 |
| ピント対応（focusModesの一覧 / API非対応） | | | `API非対応`なら制約でのAF制御は不可→解像度と撮り方で対応 |
| ズーム（範囲/現在値 / API非対応） | | | HWズーム可なら**寄り**で極小ラベルを大きく写せる |
| ライト | | | 参考 |
| レンズ（例 Back Dual Wide Camera） | | | 超広角/望遠を掴んでいたら**主カメラ固定**を検討 |
| getCapabilities | | | `非対応`＝iOSでの制約制御に制限あり、の重要判断材料 |

**極小ラベルでの体感（メモ欄）**：鉛筆など細い商品／小さいバーコードで、
（a）ピントが合うか（映像がボケないか）、（b）どのくらいの距離・ズームで読めたか、を記録。

> 目的：この数値で「解像度不足／ピント不良／レンズ選択／API制限」のどれが主因かを確定し、
> **最小の変更**で施策1・2を入れる。パネルは対策確定後に撤去する（設計判断メモ：README 不具合ログ#2続報）。

### 計測結果（v1.0.3 / 2026-07-24）と確定した結論

iPad Pro（第3世代）実測：`解像度 720×1280 / ピント API非対応 / ズーム 0.5〜10 / getCapabilities 対応 / 背面デュアル広角`
→ **映像は鮮明なのに読めず、タップ（デジタルズーム）でも改善しない**ことから、
**真因＝撮影解像度不足**と確定。ピント制御（施策1）は不要と判断（iOSで制御不可かつ合焦済み）。

## ⓪-4 v1.0.4 再計測・読取テスト（**実機必須**）

解像度を **1080p要求（`ideal:1920×1080`）** に引き上げた効果を確認する。**ズーム既定値は1×のまま**
（解像度単独の効果を切り分けるため。一度に一つの原則）。

| # | 操作 | 期待結果 | iPad Pro(3) | iPhone 16 Pro |
|---|---|---|---|---|
| V-1 | スキャン画面を開き診断パネルを見る（🔄更新も押す） | **解像度の短辺が1080相当に上がっている**（例 1080×1920）。720のままなら要エスカレーション | ☐ | ☐ |
| V-2 | 通常サイズのJAN（絵の具チューブ等）を読む | **速やかに反応し、13桁が正しく読める** | ☐ | ☐ |
| V-3 | **極小ラベル（鉛筆等の細い商品）**を読む | 読めるか／どの距離で読めるかを記録 | ☐ | ☐ |
| V-4 | 画面タップでズーム（×2〜×4）して極小ラベルを読む | 高解像度化により**今度はズームが有効に働くか**を確認 | ☐ | ☐ |
| V-5 | 反応の速さ・アプリの重さ | 高解像度化でデコードが重くなっていないか（カクつき・発熱） | ☐ | ☐ |

**記録して共有いただきたいこと**：V-1の実解像度の数値、V-2/V-3の可否、V-4でズームが効いたか、V-5の体感。

**結果別の次アクション**
- V-1で1080相当になり V-2/V-3も読める → **成功**。診断パネルを撤去して仕上げる
- V-1は上がったが極小ラベル(V-3)だけ厳しい → **既定ズーム（約2×）を追加**する次版
- V-1が720のまま → 4K相当（`ideal:2560〜3840`）要求／別の制約指定を試す
- V-5で明らかに重い → 処理フレーム間引き、または赤線周辺のみ切り出すROIデコードを検討

### v1.0.4 結果（2026-07-24）
解像度は 720×1280 → **1080×1920 に改善したが、読取は一切改善せず**（通常サイズのJANでも無反応、ピントは鮮明）。
→ **「解像度不足が主因」は誤りと確定**。以降は推測をやめ、v1.0.5で実測に切り替える。

## ⓪-5 v1.0.5 診断強化ビルドの計測（**実機必須・これで原因を確定する**）

読取ロジックは一切変更していない**計測専用**の版。**バーコードに向けた状態で📸ボタンを押す**のが要点。

**手順**
1. v1.0.5を公開 → スーパーリロード（iPhone/iPadはアイコン削除→再追加）
2. 棚卸を開始しスキャン画面へ
3. **通常サイズのJAN（絵の具チューブ等）を赤い線に合わせ、ピントが合った状態で**
   **「📸 フレーム取り込みテスト」をタップ**
4. パネルに出る全行と、**下に表示されるサムネイル画像**をそのまま共有（スクリーンショット可）
5. 続けて「🔄 診断を更新」も押し、その内容も共有

**共有いただきたい項目**

| 表示行 | 例 |
|---|---|
| 取込サイズ / 平均輝度 | `1080×1920` / `205` |
| 全画面デコード | `✅成功 4901…` or `×失敗 N` |
| 中央帯ROIデコード | `✅成功 4901…` or `×失敗 N` |
| **サムネイル画像** | **実際にZXingが受け取った画像（最重要）** |
| video要素 / ZXing取込canvas | `1080×1920 readyState=4` / `1080×1920` |
| デコード試行 / 例外内訳 / 読取後の例外 | `120回 (8.5/秒) / 成功 0回` / `N:120` / `(なし)` |

**結果の読み方（原因判定表）**

| 観測 | 判定される原因 | 次の対策 |
|---|---|---|
| **ZXing取込canvasが 0×0** | 空画像をデコードし続けている | 自前で`getUserMedia`→`videoWidth>0`待ち→`decodeFromStream`へ変更 |
| **サムネイルが真っ黒／平均輝度が0付近** | iOSでvideo→canvas描画が機能していない | `requestVideoFrameCallback`等での取込方式に変更 |
| **サムネイルは正常だが全画面デコード×** | 画像は届いているがバーコードが小さい/条件不足 | **ROI化**＋必要ならズーム既定値 |
| **全画面×だが中央帯ROIが✅** | **ROI化が解**（本命候補） | 赤線周辺のみを切り出して連続デコード |
| **デコード試行が0回** | デコードループが回っていない | `start()`の構成を見直し |
| **「読取後の例外」に内容が出る** | **読取は成功しているが後段で失敗**（無反応の正体） | その例外箇所を修正 |
| 全て正常なのに読めない | 別要因 | サムネイルを見て再検討 |

> **参考（ローカル実測）**：デスクトップChromiumでは同じコードで合成バーコードを**連続読取で正しく検出**でき、
> 取込canvasも1080×1920で正常、全画面デコード24ms・中央帯ROI 8ms・バーコード無しの全画面124ms。
> **つまりコード自体は健全で、問題はiOS Safari固有**である可能性が高い。

---

## ② 実機テスト手順（iPhone・Android 各1台で実施）

前提: GitHub Pages等でHTTPS公開済み、GASバックエンドをデプロイ済み(`DEPLOY.md`参照)、
端末の「設定」タブに担当者名・提出先URL・トークンを登録済み、「商品情報」タブでCSVマスタ取込済み。

各行の「合否」に ✅/❌ を記入し、❌は状況をメモすること。

### A. カメラ・読取(実機必須)

| # | 操作 | 期待結果 | iPhone15/iOS | Android |
|---|---|---|---|---|
| 1 | ホーム→棚卸→新しく始める→名前入力→開始 | カメラが起動し赤いガイド線が表示される | ☐ | ☐ |
| 2 | JANコードを赤線に合わせる | (Androidは)振動し、商品確認画面に商品名と価格が表示される | ☐ | ☐ |
| 3 | 数量2にして登録 | トースト表示後、自動でカメラ画面に戻り件数が増える | ☐ | ☐ |
| 5 | スキャン画面を4回タップ | ×1→×2→×3段階→等倍に戻る(右上にバッジ表示、実際に映像が拡大) | ☐ | ☐ |
| 4-torch | ライトボタン | **対応端末のみボタンが表示**され、ON/OFFで実際に点灯/消灯。非対応端末はボタンが出ない(設計判断#4) | ☐ | ☐ |

### B. オフライン・PWA(実機必須)

| # | 操作 | 期待結果 | iPhone15/iOS | Android |
|---|---|---|---|---|
| 10 | 機内モードにしてスキャン・登録 | 問題なく登録できる。ヘッダに「オフライン」バッジ(山吹色)表示 | ☐ | ☐ |
| 11 | 機内モードのまま「Webへ提出」 | 「オフラインです…」エラー。明細データは端末に残る | ☐ | ☐ |
| 17 | ホーム画面に追加→アプリを閉じ、機内モードで起動 | オフラインでもアプリが起動し、既存の棚卸データが見える | ☐ | ☐ |

### C. バックエンド連携(デプロイ済みGAS必須)

| # | 操作 | 期待結果 | 合否 |
|---|---|---|---|
| 12 | オンラインで「Webへ提出」 | スプレッドシート「棚卸データ」に明細行が追記され、「opsLog」に提出記録が残る | ☐ |
| 13 | 設定のトークンを誤った値にして提出 | 「認証に失敗しました…」エラー。スプレッドシートには追記されない | ☐ |

### D. 実機での最終確認(①で基盤確認済み・実機再確認推奨)

| # | 操作 | 期待結果 | 合否 |
|---|---|---|---|
| 6 | 読み取りをスキップ→JAN手入力 | 数字以外はエラー、正しい番号で商品確認画面へ | ☐ |
| 7 | マスタにないJANを読む/入力 | 「マスタに見つかりません」表示。名前入力欄が出て登録できる | ☐ |
| 8 | 棚卸を終了する | データ確認画面に遷移し明細一覧が出る | ☐ |
| 9 | 数量タップ→修正／🗑→削除 | 一覧と合計が更新される | ☐ |
| 14 | 商品情報→スマレジCSV取込 | 列対応が自動推定され、取込後に件数が増える | ☐ |
| 15 | Shift_JISのCSVを取込 | 文字化けせず取り込める(実SJISファイルで確認) | ☐ |
| 16 | CSV出力 | Excelで文字化けなく開ける(棚卸名/JAN/商品名/単価/数量/日時) | ☐ |

---

## 補足

- **京都銀行等の外部取込は本アプリ対象外**(棚卸はスプレッドシート追記のみ)。
- 提出は同一データを複数回押すとスプレッドシートに重複追記される仕様(設計判断#5「加算方式」と整合、
  再提出時は確認ダイアログあり)。運用では提出後の再提出可否を担当者で取り決めること。
- 実機テストで不具合を発見した場合は、CLAUDE.md/共通標準§5に従い
  「症状＋該当設計書の記述＋再検証するテスト番号」の形で記録し、README「不具合ログ」に追記する。

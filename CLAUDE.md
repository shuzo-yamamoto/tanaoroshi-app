# CLAUDE.md — tanaoroshi-app(画箋堂 棚卸PWA)

## 最初に読むもの(毎セッション)

1. `claude_app/00_GAS業務アプリ開発標準.md`(共通標準)
2. 本ファイル
3. `README.md`(設計判断ログ・不具合ログ・既知の制約)

## 絶対ルール

- 仕様の正は README.md の設計判断ログと本ファイル。矛盾したらユーザーに質問する
- **仕様外の機能追加はコード化せず提案止まり**(承認後に実装)
- 仕様変更時は先にREADME(設計判断ログ)を更新してからコードを直す
- 新しい落とし穴を踏んだら README の不具合ログに記録し、汎用的なものは標準§4へ追記

## このプロジェクト固有の前提(標準からの逸脱)

- **静的PWA + GAS API構成**。GAS HTML Service / google.script.run は使わない
  (理由: オフラインPWA・カメラ連続読取がGASサンドボックスで成立しないため。README設計判断#1)
- フロントは素のHTML/CSS/JS。フレームワーク・CDN読込禁止(オフライン耐性・費用ゼロ)
- ZXingは `js/zxing.min.js` にローカルバンドル済み(v0.21.3)。CDNに置き換えない
- 通信は `fetch` + `Content-Type: text/plain`(CORSプリフライト回避)。GAS側レスポンスは
  `{success:true, data} | {success:false, error}` 形式(標準§3.1)
- GAS側の書き込みは `LockService` 必須・一括 `setValues` のみ(標準§3.2)
- JAN・IDを書き込む列はプレーンテキスト書式を設定(標準§4-1: 日付誤変換対策)

## アーキテクチャ早見

| 層 | ファイル | 役割 |
|---|---|---|
| UI/遷移 | `index.html` `js/app.js` | 全画面(view-*)と遷移(`goto()`)・業務ロジック |
| 読取 | `js/scanner.js` | カメラ起動/停止・ズーム3段階循環・torch・2秒重複抑制 |
| データ | `js/db.js` | IndexedDB(products / sessions / items) |
| オフライン | `sw.js` | アプリシェルをキャッシュ優先配信。更新時は `CACHE_VERSION` を上げる |
| 提出先 | `gas/Code.gs` | doPost(ping / submit)。token認証・排他・opsLog |

## 変更時の注意

- `sw.js` の `CACHE_VERSION` を上げないと利用端末に更新が届きにくい。**フロントを変更したら必ず更新**
- スキャン画面を離れるときは必ず `Scanner.stop()`(`goto()` 内で実施済み。遷移経路を追加する場合も守る)
- 数量・金額の集計の正はスプレッドシート側。フロントの合計表示は参考値
- モード追加(発注・在庫確認等)は README「将来拡張の設計メモ」の手順に従う

## 検収テスト(手動チェックリスト)

デプロイ後、実機(iPhoneとAndroid各1台)で実施:

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | ホーム→棚卸→新しく始める→名前入力→開始 | カメラが起動し赤いガイド線が表示される |
| 2 | JANコードを赤線に合わせる | 振動(Android)・商品確認画面に商品名と原価が表示される |
| 3 | 数量2にして登録 | トースト表示後、自動でカメラ画面に戻り件数が増える |
| 4 | 同じ商品をもう一度読む | 「既に◯点登録済み」の注意が表示される |
| 5 | 画面を4回タップ | ×1→×2…→×3段階→等倍に戻る(バッジ表示) |
| 6 | 読み取りをスキップ→JAN手入力 | 数字以外はエラー、正しい番号で商品確認画面へ |
| 7 | マスタにないJANを読む | 「マスタに見つかりません」表示。名前入力欄が出て登録できる |
| 8 | 棚卸を終了する | データ確認画面に遷移し明細一覧が出る |
| 9 | 数量タップ→修正 / 🗑→削除 | 一覧と合計が更新される |
| 10 | 機内モードでスキャン・登録 | 問題なく登録できる(オフラインバッジ表示) |
| 11 | 機内モードで提出 | 「オフラインです」エラー。データは残る |
| 12 | オンラインで提出 | スプレッドシート「棚卸データ」に行が追記され、opsLogに記録 |
| 13 | 誤ったトークンで提出 | 「認証に失敗しました」エラー |
| 14 | 商品情報→CSV取込(スマレジCSV) | 列対応が自動推定され、取込後に件数が増える |
| 15 | Shift_JISのCSVを取込 | 文字化けせず取り込める |
| 16 | CSV出力 | Excelで文字化けなく開ける(棚卸名/JAN/商品名/原価/数量/日時) |
| 17 | ホーム画面に追加→アプリを閉じて機内モードで起動 | オフラインでも起動し既存データが見える |

# デプロイ手順書(実機テストまで)

このリポジトリのコードは実装済み。あとは①GAS②GitHub Pages③端末設定の3段階。

## ① バックエンド(Google Apps Script)

1. Googleスプレッドシートを新規作成(例:「棚卸データ 2026」)
2. スプレッドシート上部メニュー → 拡張機能 → Apps Script
3. 開いたエディタのデフォルトコードを全消しして、`gas/Code.gs` の中身を丸ごと貼り付け → 保存(フロッピーアイコン、Ctrl+S)
4. スプレッドシートに戻り、シートタブの「＋」で新しいシートを作成 → シート名を `settings` に変更(全角半角に注意、コード内の `SHEET_SETTINGS` と一致させる)
5. `settings` シートに入力:
   - A1 = `token`
   - B1 = 任意の長い英数字(32文字程度推奨。チャットで生成済みのものを使うか、新しく決めてもよい)
6. Apps Scriptエディタに戻り、右上「デプロイ」→「新しいデプロイ」
   - 歯車アイコンで種類を選択 →「ウェブアプリ」
   - 説明:任意(例: v1)
   - 次のユーザーとして実行:**自分**
   - アクセスできるユーザー:**全員**
     - ⚠️ ここを「自分のみ」や組織限定にすると、PWAからの `fetch` がログイン画面にリダイレクトされて失敗する。トークン認証(手順5)で保護しているので「全員」で問題ない
   - 「デプロイ」をクリック → Googleアカウントの権限承認が出たら許可
7. 発行された `https://script.google.com/macros/s/xxxxx/exec` のURLをメモ

**動作確認**: このURLに対して、PWA側の「設定」画面から接続テストを行うのが手順③。単体で確認したい場合はブラウザの開発者ツールなどで以下相当のPOSTを送り、`{"success":true,...}` が返ればOK。

```json
POST <exec URL>
Content-Type: text/plain
{"token":"<settingsに設定した値>","action":"ping"}
```

## ② フロントエンド(GitHub Pages)

1. GitHubで新規リポジトリを作成(Public/Privateどちらでも可。Privateの場合はGitHub Pages有料プラン必須な点に注意 → 迷ったらPublic)
2. このフォルダ(`index.html` がある階層)の中身をそのままpush
   - `files.zip` と 空の `tanaoroshi-app/` サブフォルダは不要な残骸なので、pushする前に除外してよい(削除してもよいし `.gitignore` に入れてもよい)
3. リポジトリの Settings → Pages
   - Source: `Deploy from a branch`
   - Branch: `main` / `/(root)`
   - 保存すると数分でURLが発行される(`https://<ユーザー名>.github.io/<リポジトリ名>/`)
4. 発行URLをスマホのブラウザで開けることを確認

## ③ 端末側の初期設定(各端末で1回)

1. 発行URLをスマホのブラウザで開く
2. 下部タブ「設定」
   - 担当者名を入力
   - 提出先URL = ①で発行した `…/exec`
   - 提出用トークン = ①で `settings` シートB1に設定した値
   - 「設定を保存する」→「接続テスト」を押し、「接続OK」と出ることを確認
3. 下部タブ「商品情報」→「CSVから商品マスタを取り込む」でスマレジCSVを取込
4. ホーム画面に追加(iPhone: 共有→ホーム画面に追加 / Android: メニュー→アプリをインストール)

## 実機テスト(CLAUDE.mdの検収チェックリスト)

デプロイ後、iPhone・Android各1台で以下を通しで確認:

| # | 操作 | 期待結果 |
|---|---|---|
| 1 | ホーム→棚卸→新しく始める→名前入力→開始 | カメラが起動し赤いガイド線が表示される |
| 2 | JANコードを赤線に合わせる | 振動(Android)・商品確認画面に商品名と価格が表示される |
| 3 | 数量2にして登録 | トースト表示後、自動でカメラ画面に戻り件数が増える |
| 4 | 同じ商品をもう一度読む | 「既に◯点登録済み」の注意が表示される |
| 5 | 画面を4回タップ | ×1→×2…→×3段階→等倍に戻る(バッジ表示) |
| 6 | 読み取りをスキップ→JAN手入力 | 数字以外はエラー、正しい番号で商品確認画面へ |
| 7 | マスタにないJANを読む | 「マスタに見つかりません」表示。名前入力欄が出て登録できる |
| 8 | 棚卸を終了する | データ確認画面に遷移し明細一覧が出る |
| 9 | 数量タップ→修正 / 🗑→削除 | 一覧と合計が更新される |
| 10 | 機内モードでスキャン・登録 | 問題なく登録できる(オフラインバッジ表示) |
| 11 | 機内モードで提出 | 「オフラインです」エラー。データは残る |
| 12 | オンラインで提出 | スプレッドシート「棚卸データ」に行が追記され、opsLogに記録 |
| 13 | 誤ったトークンで提出 | 「認証に失敗しました」エラー |
| 14 | 商品情報→CSV取込(スマレジCSV) | 列対応が自動推定され、取込後に件数が増える |
| 15 | Shift_JISのCSVを取込 | 文字化けせず取り込める |
| 16 | CSV出力 | Excelで文字化けなく開ける(棚卸名/JAN/商品名/単価/数量/日時) |
| 17 | ホーム画面に追加→アプリを閉じて機内モードで起動 | オフラインでも起動し既存データが見える |

## つまずきやすい点

- `sw.js` の `CACHE_VERSION` を上げないと、既にホーム画面に追加した端末に更新が届かない。フロントを1文字でも変更したら忘れずに上げる
- `settings` シートのA1/B1の値は必ずプレーンテキストで(Googleが自動で数値や日付に変換すると token 照合が失敗する)
- GASの「アクセスできるユーザー」を「全員」にし忘れると、フロントから常に認証エラー相当になる(実際はログインページのHTMLが返ってきてJSON parseに失敗する形で気づくことが多い)
